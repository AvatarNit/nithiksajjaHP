# runtime: python39

runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
    operating_system: "ubuntu22"

# service: nithiksajja

handlers:
- url: /static
  static_dir: static
- url: /.*
  script: auto

env_variables:
  admin: ''
  name: ""
  password: ""
  msgCount: 0
  test1: ""